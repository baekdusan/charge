# 수집기 상태
collector/.last-upload-hash

# 생성물 (xcodegen generate 로 재생성)
ios/*.xcodeproj

# macOS / Xcode
.DS_Store
xcuserdata/
DerivedData/
node_modules/
supabase/.temp/

# 호스팅(멀티유저) 백엔드 주소 — 출시 시 전용 프로젝트로 교체
ios/Shared/CloudConfig.txt
collector/cloud.json

# 구버전 개인 설정 잔재 — 새 수집기는 안 읽지만 실수로 커밋되지 않게 유지
collector/.env
collector/.last-payload.json
ios/Shared/DefaultGist.txt
